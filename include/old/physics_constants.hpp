#ifndef TYPES_DEFINITIONS
#define TYPES_DEFINITIONS

#include "types_definitions.hpp"

namespace phys
{
	const tb::real HBAR(0.65821);
	const tb::real VF (1.0);
	const tb::real ACC(0.144);
	
} 
#endif
